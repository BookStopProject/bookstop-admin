{
  "private": true,
  "name": "admin",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build": "preact build",
    "serve": "sirv build --port 8080 --cors --single",
    "dev": "preact watch --port 8000",
    "lint": "eslint src",
    "test": "jest"
  },
  "eslintConfig": {
    "extends": "preact",
    "ignorePatterns": [
      "build/"
    ]
  },
  "devDependencies": {
    "enzyme": "^3.11.0",
    "enzyme-adapter-preact-pure": "^4.1.0",
    "eslint": "^8.38.0",
    "eslint-config-preact": "^1.3.0",
    "preact-cli": "^3.4.5",
    "sirv-cli": "2.0.2"
  },
  "dependencies": {
    "html5-qrcode": "^2.3.8",
    "preact": "^10.13.2",
    "preact-render-to-string": "^6.0.2",
    "preact-router": "^4.1.0",
    "react-hot-toast": "^2.4.0",
    "react-query": "^3.39.3"
  }
}
